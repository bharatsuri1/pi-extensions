# Agent Notes

- Test statusline changes with `node scripts/test-statusline.mjs --plain --widths=80,120` before asking the user to reload pi.

## Development Workflow

- We develop new extensions in steps by first setting it up as local project level symlink while developing and testing, after review and testing we commit the extension to the repo.
